# Phase 5 keyword/category benchmark

Each JSONL row contains one complete segmented news sentence and exact expected category results.
The acceptance gate is strict: category precision 1.0, category recall 1.0, and every case exact.
No fuzzy, stemming, typo, or autocorrection cases belong in this fixture because those mechanisms are disabled.
