# Project runtime: CPython 3.12.x x64 only.
fastapi==0.116.1
uvicorn[standard]==0.35.0
python-dotenv==1.1.1
psycopg[binary]==3.3.4
SQLAlchemy==2.0.50
alembic==1.18.4
yt-dlp==2026.7.4
psutil==7.2.2
pytest==8.4.1
pytest-asyncio==1.2.0
httpx==0.28.1
# OCR (Phase 3) — CPU-only, CPython 3.12 x64
paddlepaddle==3.3.1
paddleocr==3.7.0
easyocr==1.7.2
opencv-python-headless==4.13.0.92
Pillow==12.3.0
numpy==2.2.6

# Exact multi-pattern keyword matching (Phase 5)
pyahocorasick==2.3.1

# Semantic canonical-story deduplication (Phase 6; CPU-only Torch is installed separately)
sentence-transformers==5.6.0

# Complete-sentence Urdu<->English translation (Phase 7, CPU-only)
ctranslate2==4.8.1
transformers==4.57.6
sentencepiece==0.2.2
